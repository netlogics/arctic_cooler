<%= form_for(fan) do |f| %>
  <% if fan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fan.errors.count, "error") %> prohibited this fan from being saved:</h2>

      <ul>
      <% fan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :blade %>
    <%= f.number_field :blade %>
  </div>

  <div class="field">
    <%= f.label :motor %>
    <%= f.number_field :motor %>
  </div>

  <div class="field">
    <%= f.label :location %>
    <%= f.number_field :location %>
  </div>

  <div class="field">
    <%= f.label :state %>
    <%= f.text_field :state %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
